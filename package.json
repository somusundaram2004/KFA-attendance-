{
  "name": "kfa-attendance-monorepo",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}

